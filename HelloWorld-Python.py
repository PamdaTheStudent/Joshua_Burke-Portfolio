#Defining the Main program
def main():
    #then the main program prints the text into the console for the user to see
    print("Hello World")

#we check if the script is being run directly and if so then we run the main program
if __name__ == "__main__":
    main()